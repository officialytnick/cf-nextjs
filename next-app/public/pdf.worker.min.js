// Minimal placeholder for pdfjs worker to satisfy pdfjs-dist workerSrc during tests
// For production, consider copying the official pdf.worker.min.js from pdfjs-dist build output.
// This file intentionally minimal; rendering in tests uses jsdom so real rendering won't be performed.
self.importScripts = self.importScripts || function() {};